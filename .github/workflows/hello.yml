name: Hello Github Actions

on: 
  push:
    branches:
      - master

jobs:
  build:
    name: Build Job
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        run:
          echo "Hello Github Actions"